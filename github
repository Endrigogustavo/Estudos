git config --global user.name "Endrigogustavo"

git config --global user.email "endrigogustavo2006@gmail.com"

git clone https://github.com/Endrigogustavo/Estudos.git

cd Estudos

"git branch // serve para ver as brenchs"

git checkout -b V2



git add . 

git commit -m ""

git push origin V2

git checkout main


git merge V2

git add .

git commit -m ""

git push origin main
